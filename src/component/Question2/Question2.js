import React from "react";

const Question2 = () => {
    return (
        <div>
            <h2>How state works in react?</h2>
            <p>
                State is a boult in object of a react component that holds some
                state i.e. value. State can be change using this.state and when
                state of a component is changed re-rendering of that component
                is occured.
            </p>
        </div>
    );
};

export default Question2;
